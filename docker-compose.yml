services:
  newspaper-extractor:
    build: .
    ports:
      - "7860:7860"
    volumes:
      - ./input:/app/input
      - ./output:/app/output
    restart: unless-stopped
    environment:
      - TOKENIZERS_PARALLELISM=false
